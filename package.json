{
  "name": "promptlab-id-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=api",
    "vercel-build": "npm run build --workspace=frontend && npm run build --workspace=api"
  }
}
